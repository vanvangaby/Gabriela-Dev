/* General Styles */
body {
    font-family: 'Fira Code', monospace;
    background-color: #1a1b27;
    color: #fff;
    margin: 0;
    padding: 0;
    text-align: center;
}

header {
    padding: 20px;
    background: linear-gradient(90deg, #00f7ff, #0077b5);
    color: #1a1b27;
}

h1 {
    font-size: 2.5em;
    margin: 0;
}

/* Blinking Cursor */
.blinking-cursor {
    font-weight: bold;
    color: #00f7ff;
    animation: blink 1s infinite;
}

@keyframes blink {
    50% {
        opacity: 0;
    }
}

/* Stats Section */
.stats {
    display: flex;
    justify-content: center;
    gap: 20px;
    margin: 20px 0;
}

.card iframe {
    width: 400px;
    height: 200px;
    border: none;
    border-radius: 8px;
    background: #000;
}

/* Skills Section */
.skills {
    margin: 40px 0;
}

.icons img {
    width: 50px;
    height: 50px;
    margin: 10px;
    transition: transform 0.2s ease;
}

.icons img:hover {
    transform: scale(1.2);
}

/* Contact Section */
.contact {
    margin: 40px 0;
}

.links {
    display: flex;
    justify-content: center;
    gap: 15px;
    margin-top: 20px;
}

.btn {
    text-decoration: none;
    padding: 10px 20px;
    border-radius: 5px;
    font-size: 1.1em;
    transition: background-color 0.3s ease, transform 0.2s ease;
}

.btn.gmail {
    background: #d14836;
    color: white;
}

.btn.linkedin {
    background: #0077b5;
    color: white;
}

.btn.instagram {
    background: #e4405f;
    color: white;
}

.btn:hover {
    transform: scale(1.1);
    filter: brightness(1.2);
}

/* Footer */
footer img {
    width: 100%;
    max-width: 600px;
    margin: 20px auto;
    display: block;
}
